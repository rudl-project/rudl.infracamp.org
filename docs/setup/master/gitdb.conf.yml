version: 1
clients:
  - name: ingress1
    accessKeysHash:
      - "<put hash of ingress1 here>"
      # Password: test
  - name: ssl_issuer1
    accessKeysHash:
    - "<put hash of >"
      # Password: test

scopes:
  - name: ssl_certs
    allowRead:
      - ingress1
      - ssl_issuer1
    allowWrite:
      - ssl_issuer1

  - name: ingress
    allowRead:
      - ingress1